handle: "youtube"
description: |
  The `youtube` agent can search and recommends youtube videos. This data can either be passed
  in or looked up from the LLM's knowledge.

  Example queries:
    - find a video on "...".
    - find a video similar to "..."

  Tags: funcs, video, youtube, media
more_info_url: "https://github.com/bahadiri/fixie-youtube"
entry_point: main:agent
public: false
